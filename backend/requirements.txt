# Зависимости однопроцессного desktop-приложения AutoSync (native_app.py) —
# без psycopg2-binary/celery/redis/gunicorn: SQLite вместо Postgres,
# ThreadPoolExecutor+APScheduler вместо Celery/Redis (см. app/config.py,
# app/services/job_queue.py).
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
requests==2.32.3
# SOAP-клиент для Rossco (see services/rossco_client.py) — их API v2.1 отдаёт
# только WSDL/SOAP, REST-обёртки нет.
zeep==4.3.3
pandas==2.2.3
openpyxl==3.1.5
xlrd==2.0.1
odfpy==1.4.1
python-docx==1.1.2
pdfplumber==0.11.4
# Обёртка над системным бинарником tesseract (см. services/ocr.py) —
# распознавание сканов/фото. Сам tesseract pip не ставит: нужен системный
# пакет tesseract-ocr + языковой пакет tesseract-ocr-rus (Debian/Ubuntu/Kali);
# на Windows — официальный установщик Tesseract-OCR (UB-Mannheim) с
# добавленным русским языком. Без tesseract функция загрузки сканов отдаёт
# понятную ошибку вместо падения, остальное приложение не затронуто.
pytesseract==0.3.13
python-dotenv==1.0.1
PyJWT==2.9.0
waitress==3.0.0
APScheduler==3.10.4
# Собственное окно приложения вместо браузера (см. native_app.py: run_window).
# На Linux использует системный webkit2gtk через PyGObject (gi) — тот НЕ
# ставится через pip, нужен системный пакет и venv с --system-site-packages
# (см. scripts/build-native-linux.sh). На Windows использует WebView2 через
# pythonnet, который pip поставит сам как зависимость pywebview.
pywebview==6.2.1
pyinstaller==6.10.0
