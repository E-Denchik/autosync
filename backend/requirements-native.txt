# Зависимости для однопроцессного запуска без Docker (native_app.py) —
# сознательно БЕЗ psycopg2-binary/celery/redis/gunicorn: они не нужны в
# этом режиме (SQLite вместо Postgres, ThreadPoolExecutor+APScheduler
# вместо Celery/Redis), а Celery с зависимостями (kombu, billiard, vine)
# заметно раздувают PyInstaller-бинарник.
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
requests==2.32.3
pandas==2.2.3
openpyxl==3.1.5
xlrd==2.0.1
odfpy==1.4.1
python-docx==1.1.2
pdfplumber==0.11.4
python-dotenv==1.0.1
PyJWT==2.9.0
waitress==3.0.0
APScheduler==3.10.4
pystray==0.19.5
Pillow==10.4.0
pyinstaller==6.10.0
